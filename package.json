{
  "name": "gatsby-remark-curlie",
  "version": "0.1.0",
  "main": "bundle.js",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "coverage": "nyc report --temp-directory=coverage",
    "test": "yarn build && jest --coverage",
    "prepublish": "yarn clean && yarn build && rollup -c",
    "format": "bsrefmt"
  },
  "repository": "git@github.com:arnau/gatsby-remark-curlie.git",
  "keywords": [
    "bucklescript",
    "reason",
    "gatsby",
    "remark"
  ],
  "author": "Arnau Siches",
  "license": "MIT",
  "devDependencies": {
    "@glennsl/bs-jest": "^0.4.3",
    "bs-platform": "^4.0.5",
    "nyc": "^12.0.2",
    "rollup": "^0.65.0",
    "rollup-plugin-commonjs": "^9.1.6",
    "rollup-plugin-node-resolve": "^3.3.0"
  },
  "dependencies": {
    "unist-util-visit": "^1.4.0"
  }
}
